<UserControl x:Class="UI.View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI;assembly=UI"
             xmlns:viewmodel="clr-namespace:ViewModels;assembly=ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="GenerationView">

    <DockPanel Height="Auto">
        <TextBlock DockPanel.Dock="Top">
            <TextBlock.Text>
                <Binding Path="UniverseSize" />
                <!--<MultiBinding StringFormat="Universe Size: {0}x{0} | Generation #: {1} | Evolution Ended?: {2}">
                    <Binding Path="UniverseSize" />
                    <Binding Path="GenerationNumber" />
                    <Binding Path="EvolutionEnded" />
                </MultiBinding>-->
            </TextBlock.Text>
        </TextBlock>
        <Button DockPanel.Dock="Top" Command="{Binding EvolveCommand}" Width="Auto">Ewolucja</Button>
        <Button DockPanel.Dock="Top" Command="{Binding ResetCommand}" Width="Auto">Resetuj Grę</Button>
        <Grid DockPanel.Dock="Bottom" Name="Board" ShowGridLines="True" Height="Auto">
        </Grid>
    </DockPanel>
</UserControl>
